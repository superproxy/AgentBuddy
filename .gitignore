# === 运行态配置（template 输出，含真实密钥，禁止提交） ===
# config/ 目录：由 template/ 模板生成的运行态文件，含真实 API Key
/config/

# 根目录本地密钥/运行态文件（禁止提交）
/llm.yaml
/mcp.yaml
env.local.yaml
opencode.json
skills-lock.json
temp_parse.py

# Codex 运行态配置（由脚本生成）
.codex/

# === 临时/缓存 ===
.DS_Store
.agents
.idea
.claude
.qoder
.qoder-cn
.trae
.ade-hermes
.mastergo
.openclaw
.specstory/
.cursor/

# 设计预览（仅本地参考，不发布）
desktop/frontend/design-previews/
design/
debug.log

# === Python ===
# 字节码文件 / 编译产物
__pycache__/
*.py[cod]
*$py.class
*.so

# 分发 / 打包
build/
dist/
*.egg-info/
*.egg

# 虚拟环境
.venv/
venv/
env/
ENV/

# 前端工程（Vue 3 + Vite）
desktop/frontend/node_modules/
desktop/frontend/dist/
desktop/frontend/*.tsbuildinfo
desktop/service/dist-ui/

# 工具缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pyc/

# 环境变量文件
.env
.env.local
.env.*.local

# npx skills add 全局安装的远程技能缓存（项目根 skills/，非 template/skills）
/skills/

# 本地测试插件配置（含真实密钥，禁止提交）
template/plugins/Plugin.plugin.yaml

# PyInstaller 构建产物
/build/
/dist/
*.spec.bak
# frozen 首次运行标记
.bundle_bootstrapped
.zcode/
# .workbuddy 运行态目录（含 mcp.json / models.json 等，由应用运行时生成）
.workbuddy/
# 各 IDE 运行时 skills 缓存目录（npx skills add 安装，与 /skills/ 同类）
.codebuddy/
.continue/
.junie/
.lingma/
.pi/
.windsurf/

# 服务端运行态数据（SQLite 数据库等，由运行时生成）
server/data/

# 设计预览 mockup（仅本地参考，不发布）
desktop/frontend/aide-redesign-mockup.html
desktop/frontend/menu-mockup.html

# SDD 工作区（task briefs / reports / ledger，短生命周期）
.superpowers/sdd/

# 调研临时目录
.dsh-ref/
.tmp-dsh/
