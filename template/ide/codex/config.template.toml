model_provider = "${LLM_ACTIVE_PROVIDER}"
model = "${OPENAI_MODEL:-gpt-5.4}"
review_model = "${OPENAI_MODEL:-gpt-5.4}"
model_reasoning_effort = "high"
disable_response_storage = true
network_access = "enabled"
windows_wsl_setup_acknowledged = true

[model_providers.${LLM_ACTIVE_PROVIDER}]
name = "${LLM_ACTIVE_PROVIDER}"
base_url = "${LLM_ACTIVE_BASE_URL}"
wire_api = "responses"
requires_openai_auth = true

[mcp_servers.my-rag]
url = "http://localhost:8060/mcp"
